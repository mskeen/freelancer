<% title @event_tracker.name %>
<% heading @event_tracker.name %>

<%= link_to "Edit", edit_event_tracker_path(@event_tracker) %> |
<%= link_to "Delete", @event_tracker, method: :delete %>

<%= @event_tracker.interval.display %>

<h4>Your unique tracker URL</h4>

<p><%= ping_event_tracker_url(@event_tracker.token) %></p>
