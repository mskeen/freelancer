require "rails_helper"

RSpec.describe EventTrackerMailer, :type => :mailer do
  # describe "alert" do
  #   let(:mail) { EventTrackerMailer.alert }

  #   it "renders the headers" do
  #     expect(mail.subject).to eq("Alert")
  #     expect(mail.to).to eq(["to@example.org"])
  #     expect(mail.from).to eq(["from@example.com"])
  #   end

  #   it "renders the body" do
  #     expect(mail.body.encoded).to match("Hi")
  #   end
  # end

  # describe "alert_cleared" do
  #   let(:mail) { EventTrackerMailer.alert_cleared }

  #   it "renders the headers" do
  #     expect(mail.subject).to eq("Alert cleared")
  #     expect(mail.to).to eq(["to@example.org"])
  #     expect(mail.from).to eq(["from@example.com"])
  #   end

  #   it "renders the body" do
  #     expect(mail.body.encoded).to match("Hi")
  #   end
  # end

end
